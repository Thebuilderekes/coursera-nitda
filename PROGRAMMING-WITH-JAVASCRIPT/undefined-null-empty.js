const word = "beach";

//write code to find match of "d"
console.log(word.match(/d/)); // null

//undefined data type can only hold undefined as a value.
// variables declared wthout assignment will have the value of undefined
